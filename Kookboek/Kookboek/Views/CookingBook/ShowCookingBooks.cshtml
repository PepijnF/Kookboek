@model IEnumerable<CookingBookModel>

@{
    var rows = Math.Ceiling(Model.Count() / 3.0);
    int column = 0;
    for (int i = 1; i <= rows; i++)
    {
        <div class="row" id="@column">
            @for ( ; column <= Model.Count() - 1; column++)
            {
                <div class="col-sm-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">@Model.ElementAt(column).Name</h5>
                            <p class="card-text">@Model.ElementAt(column).Description</p>
                            @Html.ActionLink("Show CookingBook", "ShowCookingBook", "CookingBook", new {cookingBookId=@Model.ElementAt(column).Id},new {@class="btn btn-primary"})
                        </div>
                    </div>
                </div>    
           
                if (column + 1 % 3 == 0 || column + 1 == 3)
                {
                    column += 1;
                    break;
                }
            }
        </div>
        if (i != rows)
        {
            <hr>
        }
    }
}
